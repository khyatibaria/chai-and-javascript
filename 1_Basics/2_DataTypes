"use strict" // using this statements will apply all the newer version rules of js

// alert(3+3) we are using node js and not browser so this will not work.

//code readability is very important

/* DataTypes in Js

1. number
2. bigInt
3. string -> ""
4. boolean -> true/false
5. null -> standalone valuse used when we have to purposely save empty value for a variable
6. undefined -> when no value is assigned to variable
7. symbol -> specially used to check the uniquesness
8. objects

*/

/* primitive datatypes 
7 types:- String, Number, Boolean, null, BigInt, undefined, Symbol
memory used to save these types of variable is stack memory
it is called by vaule
*/ 
let score = 100;
const scoreValue = 200;
const isLoggedIn = false;

const id = Symbol('123');
const anotherId = Symbol('123');

console.log(id === anotherId);

/* Non Premitive (Reference)
3 types:- Arrays, Objects, Functions
memory used to save these types of variable is heap memory
it is called by refernce
*/

const fruits = ['apple','mango','pineapple'];
const myObj ={
    name:"Khyati",
    age:"25"
}

const myFuction = function(){
    console.log("hey i am printing my function")
}